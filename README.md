# FinlayPackageManager

# Installation

Since this package is not in the aur you need to install it manually. To install it run `git clone https://github.com/thatmadhacker/fpm && cd fpm && makepkg -si`
